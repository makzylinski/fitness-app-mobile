import { ThemedText } from "./ui/themed-text";
import { ThemedView } from "./ui/themed-view";

export default function Toolbar() {
  return (
    <ThemedView>
      <ThemedText>Toolbar works!</ThemedText>
    </ThemedView>
  );
}
