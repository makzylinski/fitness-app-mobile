import { ThemedText } from "../ui/themed-text";
import { ThemedView } from "../ui/themed-view";

export default function RecentWorkouts() {
  return (
    <ThemedView>
      <ThemedText>Recent Workouts work!</ThemedText>
    </ThemedView>
  );
}
